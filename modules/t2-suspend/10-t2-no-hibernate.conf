# T2 Mac サスペンド安定化: ハイバネーション無効化 (/etc/systemd/sleep.conf.d/)
#
# AllowSuspendThenHibernate=yes だと、短時間サスペンドは通っても一定時間後に
# hibernate へ遷移した際に BCE コントローラが電源復帰できず復帰に失敗する。
# deep suspend のみを使う運用に固定する (mem_sleep は既に deep 選択済み)。
[Sleep]
AllowHibernation=no
AllowSuspendThenHibernate=no
